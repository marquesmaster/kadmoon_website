# Copy to deploy/odoo/.env on the VPS and fill in. Compose reads it automatically.

# Postgres password for the Odoo database (any long random string).
ODOO_DB_PASSWORD=change-this-to-a-long-random-string
