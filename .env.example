# Optional: a form-service endpoint (Formspree, Basin, Web3Forms, etc.) that
# accepts a JSON POST from the contact form. When set, the form submits there.
# When left unset, the form falls back to a prefilled mailto: to hello@kadmoon.com.
# No backend is required either way.
NEXT_PUBLIC_CONTACT_ENDPOINT=
